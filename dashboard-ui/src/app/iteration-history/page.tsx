import { StubPage } from "@/components/stub-page";

export const metadata = { title: "Iteration History — Bot Cockpit" };

export default function IterationHistoryPage() {
  return (
    <StubPage
      title="Iteration History"
      comingIn="1.5.G2"
      description="Walk-forward iteration loop ledger (Phase 9.5) — wins, losses, and ADRs per cycle. Lands in sub-block 1.5.G2."
    />
  );
}
