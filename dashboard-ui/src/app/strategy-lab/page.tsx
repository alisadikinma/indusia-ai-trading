import { StubPage } from "@/components/stub-page";

export const metadata = { title: "Strategy Lab — Bot Cockpit" };

export default function StrategyLabPage() {
  return (
    <StubPage
      title="Strategy Lab"
      comingIn="1.5.E"
      description="Backtest runs, walk-forward fold viewer, and parameter sweeps will land in sub-block 1.5.E."
    />
  );
}
