import { StubPage } from "@/components/stub-page";

export const metadata = { title: "Live Dashboard — Bot Cockpit" };

export default function DashboardPage() {
  return (
    <StubPage
      title="Live Dashboard"
      comingIn="1.5.D"
      description="Real-time equity curve, open positions, brain decisions feed, and TradingView Lightweight Charts will land in sub-block 1.5.D."
    />
  );
}
